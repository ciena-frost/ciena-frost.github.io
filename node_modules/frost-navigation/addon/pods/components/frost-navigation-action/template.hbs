{{#if hasBlock}}
	{{yield}}
{{else}}
	{{#if svgPath}}
		{{frost-svg class="icon" path=svgPath}}
        {{alias}}
		{{frost-svg class="transition-arrow" path="launch/transition-arrow"}}
	{{else}}
        {{alias}}
	{{/if}}
{{/if}}