import FrostNavigationLink from 'frost-navigation/pods/components/frost-navigation-link/component';
import layout from './template';

export default FrostNavigationLink.extend({
	layout: layout,
	classNames: ['route']
});
