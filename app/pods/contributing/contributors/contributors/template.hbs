<div class="contributor-icons">
{{#liquid-with model as |currentModel|}}
    {{#each currentModel as |contributor|}}
<div class='card'>
    {{#link-to "contributing.contributors.contributor" contributor}}
    <img class="contributor-image-small" alt="{{contributor.name}}"
             src="{{contributor.avatar_url}}" data-contributor-id={{contributor.login}} >
    {{/link-to}}
  <div class='card-block'>
      <span class='card-row card-name'>{{contributor.name}}</span>
      <span class='card-row card-handle divider'>{{contributor.login}}</span>
  </div>
</div>
    {{/each}}
  {{/liquid-with}}
</div>